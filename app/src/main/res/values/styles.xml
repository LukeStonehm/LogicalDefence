<resources>

    <!-- Base application theme. -->
    <!--possibly change to .light.-->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="actionBarStyle">@style/barTheme</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="toolbarStyle" parent="@style/ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppHasActionBar" parent="AppTheme">
        <item name="android:windowNoTitle">true</item>
        <item name="windowActionBarOverlay">false</item>
        <item name="windowActionBar">false</item>
        <item name="android:windowActionBar">false</item>
        <item name="actionBarPopupTheme">@style/ThemeOverlay.AppCompat.Dark.ActionBar</item>
    </style>

    <style name="barTheme" parent="@style/ThemeOverlay.AppCompat.Dark.ActionBar">
        <item name="android:background">@color/colorPrimary</item>
    </style>

    <style name="listItemStyle">
        <item name="android:background">?android:selectableItemBackground</item>
    </style>

    <style name="listStyle" parent="android:Widget.TextView">
        <item name="android:gravity">center_vertical</item>
        <item name="android:minHeight">100dp</item>
        <item name="android:textColor">#707070</item>
    </style>

</resources>
